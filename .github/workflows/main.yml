name: onetest
on: [push]
      
jobs:
  runtest: 
    runs-on: ubuntu-latest
    
    steps:
      - name: Git clone my repo
        uses: actions/checkout@v1 
        
      - name: NPM install
        run: npm install
        
      - name: NPM start
        run: npm start
      
      - name: STARTtest
        run: | 
          npm test
          npm coverage
